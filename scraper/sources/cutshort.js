async function searchJobs(keyword) {
  console.log(`Searching Cutshort for: ${keyword}`);

  // Cutshort API credentials will be added here
  // once API access is configured.

  console.log("Cutshort API access not configured yet.");

  return [];
}

module.exports = {
  searchJobs
};